{
  "name": "fayli",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,front -c blue,magenta \"npm --prefix api run start:dev\" \"npm --prefix front run dev\"",
    "build": "npm --prefix api run build && npm --prefix front run build",
    "install:all": "npm --prefix api install && npm --prefix front install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
